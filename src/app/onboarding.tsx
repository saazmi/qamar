// First-run flow. SPEC §10.1. Three screens max, skippable. No account, no email.

import { View, Text } from 'react-native';

export default function OnboardingScreen() {
  return (
    <View>
      <Text>Onboarding (Phase 6 stub)</Text>
    </View>
  );
}

// Settings. SPEC §10.6. Theme, script, font size, translation default, qari, budget, reminder…

import { View, Text } from 'react-native';

export default function SettingsScreen() {
  return (
    <View>
      <Text>Settings (Phase 6 stub)</Text>
    </View>
  );
}

// Review session player. SPEC §14.4.
// Two big buttons: "Récité ✓" (advances ladder) / "Difficile" (demotes one rung).

import { View, Text } from 'react-native';

export default function ReviewScreen() {
  return (
    <View>
      <Text>Review session (Phase 5 stub)</Text>
    </View>
  );
}

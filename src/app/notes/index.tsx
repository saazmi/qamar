// Notes list. SPEC §13. Searchable, filter by surah, newest first.

import { View, Text } from 'react-native';

export default function NotesScreen() {
  return (
    <View>
      <Text>Notes (Phase 5 stub)</Text>
    </View>
  );
}

// Note editor. SPEC §13. Plain text, max 5000 chars.

import { View, Text } from 'react-native';

export default function NoteEditorScreen() {
  return (
    <View>
      <Text>Note editor (Phase 5 stub)</Text>
    </View>
  );
}

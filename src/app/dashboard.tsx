// Dashboard. SPEC §10.5. Juz rings + activity heatmap + overall bar + surah mini-grid.

import { View, Text } from 'react-native';

export default function DashboardScreen() {
  return (
    <View>
      <Text>Dashboard (Phase 5 stub)</Text>
    </View>
  );
}
